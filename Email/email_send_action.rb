require 'net/smtp'

# Description: Sublayer::Action responsible for sending an email using a specified SMTP server.
# This action can be used for notifications, reports, or automated communication based on AI insights.
#
# It is initialized with the necessary email details including SMTP server settings.
# It returns a boolean indicating whether the email was sent successfully.
#
# Example usage: When you want to send an email notification or report generated by an AI process.

class EmailSendAction < Sublayer::Actions::Base
  def initialize(to:, from:, subject:, body:, smtp_server:, smtp_port:, smtp_username:, smtp_password:)
    @to = to
    @from = from
    @subject = subject
    @body = body
    @smtp_server = smtp_server
    @smtp_port = smtp_port
    @smtp_username = smtp_username
    @smtp_password = smtp_password
  end

  def call
    begin
      message = compose_message
      send_email(message)
      Sublayer.configuration.logger.log(:info, "Email sent successfully to #{@to}")
      true
    rescue StandardError => e
      error_message = "Error sending email: #{e.message}"
      Sublayer.configuration.logger.log(:error, error_message)
      raise StandardError, error_message
    end
  end

  private

  def compose_message
    <<~MESSAGE
      From: #{@from}
      To: #{@to}
      Subject: #{@subject}

      #{@body}
    MESSAGE
  end

  def send_email(message)
    Net::SMTP.start(@smtp_server, @smtp_port, 'localhost', @smtp_username, @smtp_password, :login) do |smtp|
      smtp.send_message(message, @from, @to)
    end
  end
end